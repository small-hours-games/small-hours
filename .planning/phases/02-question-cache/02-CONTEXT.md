# Phase 2: Question Cache - Context

**Gathered:** 2026-03-22
**Status:** Ready for planning

<domain>
## Phase Boundary

Wrap the existing OpenTrivia DB fetcher with a disk-based cache layer. Questions are saved to JSON files by category after fetching, and subsequent games in the same category serve questions from disk without hitting the API. Used questions are tracked per-room to avoid repeats within a session. The raw fetcher module remains unchanged.

</domain>

<decisions>
## Implementation Decisions

### Cache integration pattern
- **D-01:** Cache-through wrapper — create `src/fetcher/cached-fetcher.js` that exports the same `fetchQuestions(categoryId, amount)` signature as the raw fetcher
- **D-02:** `room.js` switches its import from `../fetcher/opentrivia.js` to `../fetcher/cached-fetcher.js` — the cache is transparent to callers
- **D-03:** The raw fetcher (`src/fetcher/opentrivia.js`) remains unchanged as a dependency of the cache module

### Cache location and structure
- **D-04:** Cache files live at `data/questions/{categoryId}.json` relative to project root
- **D-05:** Uncategorized fetches (no categoryId) use `data/questions/any.json`
- **D-06:** Each file contains the full array of decoded question objects for that category
- **D-07:** Cache directory (`data/questions/`) is created on first write, not at startup

### Used question tracking
- **D-08:** Per-room tracking — each Room instance holds a `Set<questionId>` of used question IDs
- **D-09:** When pulling from cache, filter out questions whose IDs are in the used set
- **D-10:** The used set resets when the room is destroyed (natural lifecycle cleanup)
- **D-11:** If cache has fewer unused questions than requested, fetch fresh questions from API to supplement

### Cache staleness
- **D-12:** No TTL for v2.1 — cached questions never expire automatically
- **D-13:** Cache is permanent until manually deleted (e.g., `rm -rf data/questions/`)
- **D-14:** A `clearCache(categoryId?)` utility function is exported for manual/test use

### Claude's Discretion
- File I/O approach (fs/promises vs sync — async preferred for consistency)
- Exact error handling for disk read/write failures (should degrade to fetch-through)
- Whether to lock files during concurrent writes (probably unnecessary for single-process Node)
- Test structure and mock strategy for filesystem operations

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Fetcher module (Phase 1 output)
- `src/fetcher/opentrivia.js` — Raw fetcher API: `fetchQuestions(categoryId, amount)` returns `{ ok, questions/error }`. Cache wraps this.
- `tests/fetcher/opentrivia.test.js` — Existing test patterns (vi.stubGlobal, vi.mock) — follow same style

### Session layer
- `src/session/room.js` — Line 8: current import of fetchQuestions. Line 208: call site `await fetchQuestions(config.categoryId, amount)`. Cache integration point.
- `src/session/room.js` — Room class lifecycle: constructor, destroy/cleanup — used question set lives here

### Project constraints
- `CLAUDE.md` — Zero production deps initially, earn dependencies. Node.js 22 ESM. `fs/promises` is stdlib, no extra dep needed.
- `SPEC.md` section 7.1 — Question cache spec (defines the shape and behavior this phase implements)

### Prior phase context
- `.planning/phases/01-question-fetcher/01-CONTEXT.md` — Phase 1 decisions, especially D-01 through D-04

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/fetcher/opentrivia.js` — fetchQuestions() is the upstream data source the cache wraps
- Node.js 22 `fs/promises` — native async file I/O, no dependency needed
- Question objects already have unique `id` field (`otdb_${index}_${Date.now()}`) — usable for used-question tracking

### Established Patterns
- Result wrapper: `{ ok: true, questions }` / `{ ok: false, error: { code, message } }` — cache must return the same shape
- ESM imports throughout — cache module uses `import`/`export`
- Vitest with `vi.mock()` for module mocking, `vi.stubGlobal()` for globals

### Integration Points
- `room.js` line 8: `import { fetchQuestions } from '../fetcher/opentrivia.js'` — change to `../fetcher/cached-fetcher.js`
- `room.js` line 208: `await fetchQuestions(config.categoryId, amount)` — unchanged call, cache is transparent
- Room class: add `usedQuestionIds = new Set()` property, pass to cached-fetcher or filter after fetch

</code_context>

<specifics>
## Specific Ideas

- Question IDs generated by the fetcher (`otdb_0_1711094244378`) include timestamps, so the same question fetched at different times gets different IDs. The cache should store raw API results and generate IDs on read, OR normalize IDs based on question content (e.g., hash of question text + correct answer). This needs attention during implementation.
- OpenTrivia DB rate limit is ~1 req/5s — the cache dramatically reduces API calls after warmup, which is the primary motivation.
- The `data/` directory should be added to `.gitignore` since cached questions are runtime artifacts.

</specifics>

<deferred>
## Deferred Ideas

- TTL-based cache invalidation — future milestone if API content changes significantly
- Category list caching — Phase 3 may need this for voting UI
- Background pre-fetching of popular categories — future milestone (QPOL-02)
- Session tokens for API-level deduplication — future milestone (QPOL-03)
- Cache size limits / eviction policy — unnecessary for ~24 categories of JSON

</deferred>

---

*Phase: 02-question-cache*
*Context gathered: 2026-03-22*
