---
phase: 05-card-game-gui
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "SVG card faces are visible in browser for both Shithead and Gin Rummy"
    - "Sound effects fire on TV display during card game play"
    - "Card selection (lift + glow) works on phone for both games"
    - "Facedown cards show card back image, not question mark text"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete card game GUI upgrade works visually in a browser — SVG cards render, sounds play, animations fire, and interaction states (selected, playable, unplayable, facedown) display correctly.

Purpose: Manual visual verification that the automated changes in Plans 01-02 produce the intended user experience.
Output: Confirmed working card GUI or list of issues to fix.
</objective>

<execution_context>
@/home/dellvall/small-hours/.claude/get-shit-done/workflows/execute-plan.md
@/home/dellvall/small-hours/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-card-game-gui-port-rendering-card-visuals-physics-and-sounds-from-jlogical-apps-cards-to-all-card-games/05-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of card game GUI upgrade</name>
  <files>public/host.html, public/player.html</files>
  <action>
Human verifies the complete card game GUI upgrade in a browser. This is a visual checkpoint — no code changes.

What was built: SVG card faces replace text cards in both Shithead and Gin Rummy, on both phone (player.html) and TV (host.html). Sound effects play on host display. CSS card flip, fan layout, and deal animations are defined.

Steps to verify:
1. Start the server: `npm run dev`
2. Open host display: http://localhost:3001 — create a room
3. Open phone controller in another tab: http://localhost:3001 — join the room
4. Shithead test:
   a. Start a Shithead game
   b. On phone: verify cards show SVG images (not text/Unicode symbols)
   c. On phone: tap a card — verify it lifts up with green glow (selected state)
   d. On phone: verify facedown cards show a card back image (not "?" text)
   e. On TV: verify pile top card shows as large SVG image
   f. On TV: verify player face-up cards show as small SVG images
   g. On TV: play a card — listen for "place" sound effect
   h. On TV: draw a card — listen for "draw" sound effect
5. Gin Rummy test:
   a. Start a Gin Rummy game (needs exactly 2 players)
   b. On phone: verify hand cards are SVG images
   c. On phone: tap to select a card — verify selection state
   d. On TV: verify discard top shows as SVG image
   e. On TV: draw from stock — listen for "draw" sound
   f. On TV: discard a card — listen for "place" sound
6. Edge cases:
   a. Verify no broken image icons (all SVG paths resolve)
   b. Verify cards have proper dimensions (not oversized or tiny)
   c. Verify the page loads without console errors related to cards.js
  </action>
  <read_first>
    - public/js/cards.js
    - public/host.html (first 20 lines to verify script tag)
    - public/player.html (first 20 lines to verify script tag)
  </read_first>
  <verify>User confirms visual inspection passed</verify>
  <acceptance_criteria>
    - User confirms SVG cards render on phone for Shithead
    - User confirms SVG cards render on phone for Gin Rummy
    - User confirms SVG cards render on TV for both games
    - User confirms at least one sound effect plays on TV
    - User confirms no broken image icons visible
    - User confirms card selection (lift + glow) works
  </acceptance_criteria>
  <done>User types "approved" confirming all visual checks pass, or describes issues for a fix iteration.</done>
  <resume-signal>Type "approved" if everything looks good, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all card rendering and sound effects across both games and both display types.
</verification>

<success_criteria>
User confirms SVG cards render correctly, sounds play, and interaction states work in both Shithead and Gin Rummy on both phone and TV displays.
</success_criteria>

<output>
After completion, create `.planning/phases/05-card-game-gui-port-rendering-card-visuals-physics-and-sounds-from-jlogical-apps-cards-to-all-card-games/05-03-SUMMARY.md`
</output>
