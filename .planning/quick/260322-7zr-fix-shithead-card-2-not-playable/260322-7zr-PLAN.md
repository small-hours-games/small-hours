---
phase: quick
plan: 260322-7zr
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/games/shithead.js
  - src/engine/games/shithead.test.js
  - public/player.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "A rank-2 card can be played on any pile regardless of pile top rank"
    - "A rank-2 card is shown as playable (not grayed out) in the player UI"
    - "After playing a rank-2, the next player can play any card"
  artifacts:
    - path: "src/engine/games/shithead.test.js"
      provides: "Regression tests for rank-2 special card behavior"
    - path: "src/engine/games/shithead.js"
      provides: "Fixed play validation for rank 2"
  key_links:
    - from: "src/engine/games/shithead.js:canPlayOnPile"
      to: "playCards action handler"
      via: "rank validation before pile push"
      pattern: "canPlayOnPile.*rank.*pile"
    - from: "public/player.html:canPlayCard"
      to: "card rendering with playable/unplayable class"
      via: "client-side playability check mirrors server rules"
      pattern: "canPlayCard"
---

<objective>
Fix the rank-2 card in Shithead so it is always playable on any pile.

Purpose: The 2 is a special reset card in Shithead -- it can be played on any pile and resets the effective pile rank to "anything goes" for the next player. Currently reported as not playable.
Output: Working rank-2 card with regression tests preventing future breakage.
</objective>

<execution_context>
@/home/dellvall/dot-claude/game/.claude/get-shit-done/workflows/execute-plan.md
@/home/dellvall/dot-claude/game/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/engine/games/shithead.js
@public/player.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write regression tests for rank-2 card playability and fix the bug</name>
  <files>src/engine/games/shithead.test.js, src/engine/games/shithead.js, public/player.html</files>
  <behavior>
    - Test: rank 2 can be played on empty pile (should succeed, return new state)
    - Test: rank 2 can be played on pile with high card (e.g., rank 14/Ace on top)
    - Test: rank 2 can be played on pile with rank 7 on top (7 normally restricts to <= 7)
    - Test: rank 2 can be played on pile with rank 10 on top
    - Test: rank 2 can be played on pile that already has 2s on top
    - Test: after rank 2 is played, getTopRank returns null (skipping 2s), so next player can play anything
    - Test: playing multiple 2s at once succeeds
    - Test: canPlayOnPile(2, pile) returns true for every possible pile state
    - Test: playCards action with a rank-2 card returns valid new state (no error event)
  </behavior>
  <action>
    1. Create `src/engine/games/shithead.test.js` with Vitest tests covering all the behaviors above.

    2. INVESTIGATE THE BUG: The static code for both `canPlayOnPile` (engine line 41: `if (cardRank === 2) return true`) and `canPlayCard` (player.html line 1498: `if (rank === 2) return true`) appear correct. The bug must be in one of these areas:
       - A type coercion issue (rank as string "2" vs number 2 after JSON serialization)
       - The `playCards` action handler rejecting the play before `canPlayOnPile` is called
       - The `getTopRank` function returning an unexpected value in some pile configuration
       - The frontend `canPlayCard` receiving unexpected arguments from the view data
       - An edge case in face-up or face-down card play paths

    3. Run the tests RED first. If all tests pass (the engine code is actually correct), then the bug is purely in the frontend `canPlayCard` or in how `pileTopRank` / card data flows from server to client. In that case:
       - Check if `c.rank` could ever be a string instead of a number (JSON parse issue)
       - Check if `pileTopRank` from the view could be the raw pile-top rank instead of the effective rank
       - Add defensive handling: in `canPlayCard`, use `parseInt(rank, 10) === 2` or `rank == 2` to handle type coercion
       - In `canPlayOnPile`, add similar defensive handling

    4. Fix whatever the root cause is. Ensure all tests pass GREEN.

    5. If the engine code is correct and the bug is client-side only, still add the engine tests as regression protection, and fix the client-side `canPlayCard` in `public/player.html`.
  </action>
  <verify>
    <automated>cd /home/dellvall/small-hours && npx vitest run src/engine/games/shithead.test.js</automated>
  </verify>
  <done>
    - All rank-2 playability tests pass
    - Root cause identified and fixed (documented in commit message)
    - Both engine canPlayOnPile and frontend canPlayCard handle rank 2 correctly
    - No regressions in other card rank behavior
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/engine/games/shithead.test.js` passes all rank-2 tests
- Manual trace: construct a game state with a high card on pile, attempt to play a rank-2 card, verify it succeeds
</verification>

<success_criteria>
- Rank-2 cards are always playable on any pile (engine validation passes)
- Rank-2 cards appear as playable in the player UI (not grayed out)
- Regression test file exists preventing future breakage
- Root cause documented
</success_criteria>

<output>
After completion, create `.planning/quick/260322-7zr-fix-shithead-card-2-not-playable/260322-7zr-SUMMARY.md`
</output>
