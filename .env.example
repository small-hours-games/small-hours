# If set, this domain is used for QR codes and displayed URLs instead of the local IP.
# The server still listens on the local network — the domain must resolve to this machine.
DOMAIN=quiz.yourdomain.com

# Port to listen on (default: 3000)
PORT=3001
